apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
- deployment.yaml
- service.yaml
- ingress.yaml

namePrefix: whoami-

labels:
- includeSelectors: true
  pairs:
    app.kubernetes.io/name: whoami


helmCharts: []
